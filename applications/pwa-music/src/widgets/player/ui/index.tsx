import { VFC } from 'react';
import { PlayerControl } from '../../../features/player-control';

export const Player: VFC = () => {
    return (
        <div>
            <PlayerControl></PlayerControl>
        </div>
    );
};
